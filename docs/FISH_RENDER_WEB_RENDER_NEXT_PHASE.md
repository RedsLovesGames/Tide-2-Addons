# Next phase

After the Fish Wiki layout is fixed, convert each entry in `supported-fish-registry.json` to normalized browser geometry/material/layer data. Preserve Tide DisplayData and all source-mod variant/emissive logic. Use the extracted real model sources, never item sprites as geometry.
